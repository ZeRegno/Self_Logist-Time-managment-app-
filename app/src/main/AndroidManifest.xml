<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.selflogist">

    <!-- Разрешения, необходимые для приложения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Другие разрешения, если они необходимы -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Selflogist">

        <activity android:name=".ui.AddTaskActivity" />
        <activity android:name=".ui.CalendarActivity" />
        <activity android:name=".ui.DayScheduleActivity"> </activity>

        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>





        <activity android:name=".AddTaskActivity" />
        <activity android:name=".TaskViewModel" />

    </application>

</manifest>